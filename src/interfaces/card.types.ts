export interface PracticeCard {
  id : number;
  title: string;
  description: string;
  active: boolean;
}

export interface CardProps {
  id : number;
  title: string;
  description: string;
  active: boolean;
}
